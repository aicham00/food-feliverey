specVersion: "v1beta"
serviceId: "food delivery"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "food delivery-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
